[phases.setup]
nixPkgs = ["gradle", "jdk17"]

[phases.build]
cmds = ["./gradlew :backend:build -x test"]

[phases.start]
cmd = "java -jar -Dspring.profiles.active=production backend/build/libs/backend-0.0.1-SNAPSHOT.jar"